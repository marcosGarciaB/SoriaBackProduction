# ============================================
# CONFIGURACIÓN DE BASE DE DATOS
# ============================================

# URL completa de conexión a MySQL
# Formato: jdbc:mysql://HOST:PORT/DATABASE?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# Para desarrollo local con docker-compose: jdbc:mysql://mysql:3306/soria?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# Para Railway: jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
DATABASE_URL=jdbc:mysql://localhost:3306/soria?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Usuario de la base de datos
DATABASE_USERNAME=root

# Contraseña de la base de datos
DATABASE_PASSWORD=rootpassword

# Nombre de la base de datos (solo para docker-compose)
DATABASE_NAME=soria

# ============================================
# CONFIGURACIÓN JWT
# ============================================

# Clave secreta para firmar tokens JWT
# IMPORTANTE: Debe ser una cadena larga y segura. Genera una con: openssl rand -base64 32
# NO uses el valor por defecto en producción
JWT_SECRET=changeme

# Tiempo de expiración del token JWT en milisegundos
# 3600000 = 1 hora, 86400000 = 24 horas
JWT_EXPIRATION=3600000

# ============================================
# CONFIGURACIÓN CORS
# ============================================

# Orígenes permitidos separados por comas
# Ejemplo: http://localhost:8081,http://localhost:8082,https://tu-dominio.com
CORS_ALLOWED_ORIGINS=http://localhost:8081,http://localhost:8082

# ============================================
# CONFIGURACIÓN JPA/HIBERNATE
# ============================================

# Modo de DDL de Hibernate
# Opciones: create, create-drop, update, validate, none
# - create: Crea las tablas al iniciar (borra datos existentes)
# - update: Actualiza el esquema sin borrar datos (recomendado para producción)
# - validate: Solo valida el esquema sin hacer cambios
# - none: No hace nada
JPA_DDL_AUTO=update

# Mostrar SQL en los logs (útil para desarrollo, desactivar en producción)
JPA_SHOW_SQL=false

# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================

# Puerto donde corre la aplicación
# Railway asigna automáticamente el puerto, pero puedes especificarlo aquí
PORT=8080

# ============================================
# CONFIGURACIÓN DE LOGGING
# ============================================

# Nivel de logging
# Opciones: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# ============================================
# INSTRUCCIONES
# ============================================
# 1. Copia este archivo como .env en la raíz del proyecto
# 2. Ajusta los valores según tu entorno (desarrollo, producción, etc.)
# 3. NUNCA commitees el archivo .env al repositorio (debe estar en .gitignore)
# 4. Para Railway, configura estas variables en el panel de configuración del servicio

